pipeline {
    agent any

    stages {
        stage("SCM Checkout") {
            steps {
                git branch: 'main',
                    credentialsId: "github-access",                
                    url: "https://github.com/avbravikanth1/my-react-app.git"            
            }
        }
        stage('Build') {
            steps {
                sh "~/Desktop/ravi/my-react-app"
                
            }
        }
        
}
/*stage('Clean Up') {
            steps {
                sh "sh script.sh"
            }
        }*/